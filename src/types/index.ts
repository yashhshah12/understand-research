export interface NormalizeData {
    paperId ?:string,
    author ?:string,
    title ?: string,
    source ?:string,
    abstract ?:string,
    url ?:string,
    publishDate ?:string,
    category ?:string,
    year?: number;
}